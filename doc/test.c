#include <stddef.h>
#include <stdio.h>
void print(int arg) { printf("%d", arg); }
int input() {
  int n;
  scanf("%d", &n);
  return n;
}
size_t res_queue[32];
size_t res_count;
size_t getres(){
    if(res_count)
}
size_t end();
int main(int argn, char **argv) { end(); }
size_t F(size_t x) {
  size_t res;
  if (((*(int *)x == 1.1) | (*(int *)x == 2.1))) {
    *(int *)res = 1;
  } else {
  };
  *(int *)res = (F((*(int *)x - 1)) + F((*(int *)x - 2)));
  return res;
};
size_t end() {
  size_t res;
  print(F(input()));
  *(int *)res = 0;
  return res;
};
